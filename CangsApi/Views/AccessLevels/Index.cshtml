@model IEnumerable<CangsApi.Models.AccessLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.levDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.levDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.levelNum }) |
            @Html.ActionLink("Details", "Details", new { id=item.levelNum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.levelNum })
        </td>
    </tr>
}

</table>
